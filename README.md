# MNU Events Platform

Платформа для управления университетскими мероприятиями, разработанная для студентов и организаторов.

**Статус:** MVP (Minimum Viable Product). Готов к запуску после решения критических проблем безопасности.

| Метрика | Статус | Цель (MVP) |
|---|---|---|
| **Безопасность** | 4.5/10 (8 критических проблем) | 6.5/10 (3 решены) |
| **Функционал** | 82% (базовый MVP) | 95% (с монетизацией) |
| **Монетизация** | 0% (только mock-оплата) | 80% (ручная оплата + реклама) |
| **Роли** | 3 (STUDENT, ORGANIZER, ADMIN) | 4 (+MODERATOR) |
| **Production Ready** | ❌ НЕТ | ⚠️ УСЛОВНО |

---

## 🚀 Быстрый запуск

### Требования
- Node.js 20+
- Docker & Docker Compose
- `npm` (обычно идет в комплекте с Node.js)

### Автоматический запуск (Рекомендуется)
```bash
# Дать права на выполнение скрипту
chmod +x start.sh
# Запустить
./start.sh
```
Этот скрипт автоматически запускает базу данных, устанавливает зависимости, применяет миграции и запускает frontend и backend серверы.

### Ручной запуск
1.  **Запустить базу данных (Терминал 1):**
    ```bash
    docker-compose up -d
    ```
2.  **Настроить и запустить Backend (Терминал 2):**
    ```bash
    cd backend
    npm install
    # Для пользователей WSL, если возникают проблемы с bcrypt
    npm rebuild bcrypt
    # Скопировать переменные окружения
    cp .env.example .env
    # Применить миграции базы данных
    npx prisma migrate dev
    # Сгенерировать Prisma-клиент
    npx prisma generate
    # Заполнить БД тестовыми данными
    npx prisma db seed
    # Запустить сервер
    npm run start:dev
    ```
3.  **Запустить Frontend (Терминал 3):**
    ```bash
    # Установить зависимости (в корне проекта)
    npm install
    # Запустить dev-сервер
    npm run dev
    ```

### Доступы
-   **Frontend:** `http://localhost:5173`
-   **Backend API:** `http://localhost:3001`
-   **API Документация (Swagger):** `http://localhost:3001/api/docs`

---

## 🛠️ Конфигурация и разработка

### Конфигурация окружения (`backend/.env`)

Создайте файл `backend/.env`, скопировав его из `backend/.env.example`.

#### Аутентификация и Безопасность
```bash
# Используется для подписи JWT токенов. Сгенерируйте надежный, случайный ключ.
JWT_SECRET=your-secret-key-change-in-production
REFRESH_TOKEN_SECRET=your-refresh-secret-change-in-production
EMAIL_VERIFICATION_SECRET=your-verification-secret-change-in-production

# 🆕 Секрет для подписи данных, связанных с платежами (например, QR-кодов).
# ДОЛЖЕН быть надежной, случайной строкой.
PAYMENT_SECRET=your-payment-secret-key

# Время жизни токенов
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_IN=7d
```

#### Конфигурация SMTP (Опционально)
```bash
# Для отправки email (верификация, уведомления)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
```

#### ⚠️ Рекомендации по безопасности
-   **Никогда** не коммитьте `.env` файлы в систему контроля версий.
-   **Никогда** не используйте примеры секретных ключей в production.
-   Генерируйте случайные, надежные секреты (32+ символов).

### Сидирование базы данных

Скрипт для заполнения БД находится в `backend/prisma/seed.ts`.

**Запуск сидирования:**
```bash
npx prisma db seed
```

**Важные моменты:**
-   Скрипт создает тестовые аккаунты для ролей `ADMIN`, `ORGANIZER`, `MODERATOR` и `STUDENT`.
-   Создает начальные данные для `PricingTier`, позиций `Advertisement` и других новых функций.
-   Скрипт идемпотентен (безопасен для многократного запуска).
-   Для изменения тестовых данных отредактируйте `backend/prisma/seed.ts`.

### Тестовые аккаунты (после сидирования)
-   **Admin:** `admin@kazguu.kz` / `Password123!`
-   **Organizer:** `organizer@kazguu.kz` / `Password123!`
-   **Moderator:** `moderator@kazguu.kz` / `Password123!`
-   **Student:** `student1@kazguu.kz` / `Password123!`

---

## 🗺️ План развития (Roadmap)

Проект требует доработки для production-использования. План разделен на фазы.

### Фаза 1: Критические исправления безопасности (3 дня)
**Цель:** Устранить уязвимости, блокирующие запуск.
-   [ ] **Webhook Signature Verification:** Добавить HMAC-проверку для защиты от поддельных запросов.
-   [ ] **IDOR Fix в QR Scanner:** Организатор должен иметь возможность сканировать QR-коды только своих мероприятий.
-   [ ] **Race Conditions Fix:** Использовать транзакции для предотвращения переполнения мероприятий.

### Фаза 2-3: Система модерации (7 дней)
**Цель:** Внедрить модерацию контента.
-   [ ] **Роль MODERATOR:** Новая роль для управления контентом.
-   [ ] **Очередь модерации:** API и UI для одобрения/отклонения объявлений и мероприятий.
-   [ ] **Технические фильтры:** Автоматические проверки контента (минимальная длина, повторы).

### Фаза 4: Монетизация (12 дней)
**Цель:** Внедрить все запланированные источники дохода.
-   [ ] **Гибкие тарифы:** Настройка цен для платных мероприятий от внешних организаторов.
-   [ ] **Система рекламы:** Баннеры и нативная реклама с отслеживанием показов.
-   [ ] **Платные мероприятия:** Механизм ручного подтверждения оплаты через Kaspi (загрузка чека).
-   [ ] **Премиум-подписка:** Расширенные лимиты для студентов за ежемесячную плату.

### Фаза 5: Геймификация (8 дней)
**Цель:** Повысить вовлеченность студентов.
-   [ ] **Система баллов:** Начисление баллов за посещение мероприятий и активность в клубах.
-   [ ] **Уровни и достижения:** Внедрение системы уровней (Новичок, Активист, Лидер, Легенда) и значков.

### Фаза 6: Тестирование и запуск (5 дней)
**Цель:** Обеспечить стабильность и подготовиться к запуску.
-   [ ] **E2E и нагрузочное тестирование:** Проверка ключевых сценариев.
-   [ ] **Настройка Production-окружения:** Docker, SSL, переменные окружения.
-   [ ] **Финальная проверка и запуск.**

---

**Последнее обновление:** 2025-11-19
**Версия:** 3.0 (MVP Setup Guide)
